# Survey: adopt this, write that (2026-09-01)

Input to keystone's day-one shape. Sources are primary docs and READMEs.

## Adopt (use as-is)
- Claude Code `/goal` plus Stop-hook gate for loop-until-done; condition must name a check. https://code.claude.com/docs/en/goal
- `ralph-wiggum` plugin for the fixed-prompt loop. https://github.com/anthropics/claude-code/tree/main/plugins/ralph-wiggum
- Bundled `/verify` (drives the running app), `/run`, `/code-review` (fresh-context reviewer). https://code.claude.com/docs/en/skills
- Playwright MCP `--headless` for screenshots; claude-in-chrome is visible-window only. https://github.com/microsoft/playwright-mcp
- Plan mode and the explore/plan/implement/commit workflow. https://code.claude.com/docs/en/best-practices
- Plugin packaging and marketplace format. https://code.claude.com/docs/en/plugins

## Adapt (borrow the shape)
- spec-kit `spec-template.md`: independent test per story, Given/When/Then, `[NEEDS CLARIFICATION]` markers. Change: replace success-criteria prose with a runnable check per item. https://github.com/github/spec-kit
- Kiro: requirement-to-task traceability tags. Change: tag checklist items with the decision they serve. https://kiro.dev/docs/specs/feature-specs/
- Anthropic long-running-agent harness: feature list as JSON with `passes:false`, a verify gate that blocks marking pass without evidence, an evaluator with no write tools. Change: generate from the spec checklist if markdown proves editable-by-accident. https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents
- spec-kit constitution and BMAD project-context: ~400 words, principles with severity, only what is expensive to rediscover. Change: keystone's loop standard is the workflow section; boxes are what both lack. https://github.com/github/spec-kit/blob/main/templates/constitution-template.md
- `.claude/rules/*.md` with `paths:` globs to load rules by path; under 200 lines each. https://code.claude.com/docs/en/memory
- Anthropic skill-creator eval shape: `evals.json` cases, `grading.json` with evidence. https://github.com/anthropics/claude-plugins-official

## Write (nothing covers it)
- Boxes as decision procedures. Every surveyed standards file is a preference list.
- Per-item runnable check bound to a checklist item and executed by the gate.
- One document that says what "verified" means.

## Notable
- Separate the worker from the judge. Self-evaluation fails. https://www.anthropic.com/engineering/harness-design-long-running-apps
- Evidence, not assertion.
- One item per loop, fresh context; git log as memory.
- Clarify before plan.
- Harness complexity should decay: every component encodes an assumption about what the model can't do. Date everything, prune on use.
